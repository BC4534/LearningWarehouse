
    def test_{{title}}():
        response = requests.options(
            method = {{method}},
            url = {{url}},
            headers={{headers}},
            cookies={{cookies}},
            data={{data}},
            json=json,

        )
        assert response.status_code == 200
